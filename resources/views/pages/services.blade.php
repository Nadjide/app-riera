<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <title>{{config('app.name', 'AppRiera')}}</title>

    </head>
    <body>
            <h1>Voici la pages services</h1>
            <p>c'est la pages service </p>
    </body>
</html>
